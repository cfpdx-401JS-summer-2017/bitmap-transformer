// assert.ok(constants.PIXEL_OFFSET);
// assert.ok(constants.BITS_PER_PIXEL_OFFSET);
// assert.ok(constants.FILE_SIZE_OFFSET);

const PIXEL_OFFSET = '';
const BITS_PER_PIXEL_OFFSET = '';
const FILE_SIZE_OFFSET = '';

module.exports = {
  PIXEL_OFFSET,
  BITS_PER_PIXEL_OFFSET,
  FILE_SIZE_OFFSET
}
